package com.tupaz.ui.components

import androidx.compose.runtime.Composable

@Composable
fun BottomNavBar(
    currentRoute: String = "",
    onNavigate: (String) -> Unit = {}
) {
    // Bottom navigation bar omitted per user requirement
}
